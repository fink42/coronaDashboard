enableBookmarking(store = "server")

# Settings ----
# SQL settings
sql_settings <- list(
  # SQL username
  user = "root",
  # SQL password
  password = "secretPassword42",
  # SQL host
  host = '172.17.0.1',
  # SQL port
  port = 3308
)